function pdf(arr,word){
    word.split('').forEach((ele,index) => {
      // console.log( word.charCodeAt(index)-96)
       console.log(index)
    });
}
 
pdf(2,"abc")
 //incomplete solution its a an easy problem
 
